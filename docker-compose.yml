version: "3.8"

services:
  # Discord Music Bot
  bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - LAVALINK_URI=http://lavalink:2333
    depends_on:
      - lavalink
    networks:
      - lavalink

  # Lavalink Audio Server
  lavalink:
    image: ghcr.io/lavalink-devs/lavalink:4
    container_name: lavalink
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - _JAVA_OPTIONS=-Xmx512M
      - SERVER_PORT=2333
      - LAVALINK_SERVER_PASSWORD=${LAVALINK_PASSWORD:-youshallnotpass}
      - YOUTUBE_REFRESH_TOKEN=${YOUTUBE_REFRESH_TOKEN}
    volumes:
      - ./application.yml:/opt/Lavalink/application.yml
      - ./plugins:/opt/Lavalink/plugins
    ports:
      - "2333:2333"
    networks:
      - lavalink

  # yt-cipher (remote cipher server for YouTube signature fixes)
  yt-cipher:
    image: ghcr.io/kikkia/yt-cipher:latest
    container_name: yt-cipher
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - API_TOKEN=${YTCIPHER_TOKEN}
      - PORT=8001
      - HOST=0.0.0.0
    ports:
      - "8001:8001"
    networks:
      - lavalink

networks:
  lavalink:
    name: lavalink
